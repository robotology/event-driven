CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

SET(PROJECTNAME "OptFlowViewer")
project(${PROJECTNAME})

FIND_PACKAGE(YARP)
#FIND_PACKAGE(ICUB)

INCLUDE_DIRECTORIES(${YARP_INCLUDE_DIRS}
                    ${ICUB_INCLUDE_DIRS})


FILE (GLOB source_folder *.cpp)
FILE (GLOB header_folder *.h)

add_executable(${PROJECTNAME} ${source_folder} ${header_folder})
TARGET_LINK_LIBRARIES(${PROJECTNAME} ${YARP_LIBRARIES})

#INSTALL(TARGETS ${PROJECT_NAME} RUNTIME DESTINATION ${CMAKE_CURRENT_SOURCE_DIR}/../bin)
install(TARGETS ${PROJECTNAME} DESTINATION bin)

